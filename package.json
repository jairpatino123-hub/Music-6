 (cd "$(git rev-parse --show-toplevel)" && printf '%s' 'diff --git a/package.json b/package.json
new file mode 100644
index 0000000000000000000000000000000000000000..a0889f72b2516bb7290056a5484263a5b981838b
--- /dev/null
+++ b/package.json
@@ -0,0 +1,14 @@
+{
+  "name": "synthetiq-youtube-connector",
+  "version": "1.0.0",
+  "private": true,
+  "description": "A secure YouTube OAuth and playlist connector for the Synthetiq Music app.",
+  "type": "module",
+  "scripts": {
+    "start": "node src/server.js",
+    "dev": "node --watch src/server.js",
+    "test": "node --test",
+    "check": "node --check src/server.js && node --check src/youtube.js && node --check public/synthetiq-youtube.js"
+  },
+  "engines": { "node": ">=20" }
+}
' | git apply --3way)
